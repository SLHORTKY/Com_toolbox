#pragma once 
#include "Sig.h"

namespace Com
{
    class Filter : protected Sig
    {
    private:
        
    public:
        Filter(/* args */);
        ~Filter() = default;
    };
       
} // namespace Com
